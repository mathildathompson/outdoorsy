deleteOverlays();
$('#droparea ul').remove();

$('#restaurantslist').html("");

<% @restaurants.each do |restaurant| %>

  $('#restaurantslist').append('<%= escape_javascript(render restaurant, :locals => {:restaurant => @restaurant}) %>');

<% end %>

$('#meetups').html("");

<% @meetups.each do |meetup| %>

  $('#meetups').append('<%= escape_javascript(render meetup, :locals => {:meetup => @meetups}) %>');

<% end %>



$('#parkappend').html("");

<% @parks.each do |park| %>

  $('#parkappend').append('<%= escape_javascript(render park, :locals => {:outdoor_area => park}) %>');

<% end %>

 

 <% @restaurants.each do |item| %>
    addMarker(<%= item.latitude %>,<%= item.longitude %>,'<%= item.name %>');
 <% end %>
  <% @meetups.each do |item| %>
    addMarker(<%= item.latitude %>,<%= item.longitude %>,'<%= item.name %>');
 <% end %>
  <% @parks.each do |item| %>
    addMarker(<%= item.latitude %>,<%= item.longitude %>,'<%= item.name %>');
 <% end %>
zoomin(latlng);
latlng = [];
$('#droparea h3').show();
